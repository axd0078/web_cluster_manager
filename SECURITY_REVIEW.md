# 安全审查与缺陷记录

审查日期：2026-07-24

修复复核日期：2026-07-25

审查范围：FastAPI Server、Windows/Linux Agent、Vue 前端、Agent WebSocket、文件传输、Docker 宿主管理、任务、更新与可选远程命令。

## 结论

本次审查确认的 9 个产品安全问题已在 2026-07-25 完成修复并增加回归测试：

- 中危 5 个，已修复 5 个；
- 低危 4 个，已修复 4 个；
- 未发现高危或严重问题；
- 原有 2 个待验证项中，Agent 请求节点绑定已作为纵深防御一并修复；登录限流容量仍需结合生产部署验证。

本文件保留原始问题、影响和修复要求，作为审查记录；关闭证据见下表。

| 编号 | 状态 | 修复与回归证据 |
| --- | --- | --- |
| SEC-001 | 已修复 | WebSocket 绑定用户、角色、会话版本和过期时间；登出/改密主动断开；旧 Cookie 和活动连接均有回归测试 |
| SEC-002 | 已修复 | 前端连接保存 principal；特权 Agent 原始响应不再广播；operator 任务事件按任务所有者定向，admin 可查看全局 |
| SEC-003 | 已修复 | Agent 响应不再展开到可信字段；任务事件采用固定结构；节点和请求 ID 由服务端赋值 |
| SEC-004 | 已修复 | schema v5 把可解析的用户名迁移为用户 ID；任务创建使用用户 ID；读取、取消和重试执行 owner-or-admin |
| SEC-005 | 已修复 | Agent 持久统计已提交文件、会话预留与孤立分块；串行预留；检查聚合上限和最低剩余空间；取消及过期回收 |
| SEC-006 | 已修复 | 文件上传和传输历史统一为“管理员全局、其他用户仅本人” |
| SEC-007 | 已修复 | 未知用户名使用同一 bcrypt 策略的虚拟哈希，每次请求恰好执行一次校验 |
| SEC-008 | 已修复 | Docker 子进程双流并发分块读取，解码前共享字节预算，超限/超时杀进程并排空管道 |
| SEC-009 | 已修复 | 自由命令和 shell 执行器已移除；批量命令只接受本地固定 argv 别名，并复用有界读取器 |

## P2：中危安全问题

### SEC-001：WebSocket 会话撤销失效

- 位置：`server/api/ws.py`
- 现象：WebSocket 握手没有校验 JWT 中的会话版本，连接建立后也不复核过期时间或撤销状态。
- 影响：被复制的旧 Cookie 在登出后仍可重新连接；已经建立的连接可继续接收事件。
- 修复要求：
  - 握手阶段执行与 REST 相同的 `token_version` 校验；
  - 连接绑定用户、会话版本和过期时间；
  - 登出、修改密码、用户禁用和令牌过期时主动断开连接。
- 验收测试：
  - 登出后的 Cookie 无法重新连接；
  - 已建立连接在会话撤销或过期后被关闭。

### SEC-002：特权 Agent 结果广播给 viewer

- 位置：`server/api/ws.py`、`server/core/connection_manager.py`
- 现象：所有前端角色共用裸 WebSocket 集合，Agent 返回的日志、终端、浏览和更新结果会广播给所有连接。
- 影响：viewer 可能收到只允许管理员或操作员发起的操作结果。
- 修复要求：
  - 连接管理器保存经过认证的 principal；
  - 结果默认只返回请求发起者；
  - 对需要全局展示的状态使用独立、脱敏、显式授权的事件模型。

### SEC-003：Agent 可覆盖可信事件标识

- 位置：`server/api/ws.py`
- 现象：不可信 `payload` 在可信 `node_id`、`request_id` 后展开，同名字段可覆盖服务端值。
- 影响：被攻陷的 Agent 可以在管理界面中伪装为其他节点，误导操作员判断操作结果。
- 修复要求：
  - 禁止 Agent payload 使用保留字段；
  - 使用类型化事件结构；
  - 服务端可信字段最后赋值并在广播前校验。

### SEC-004：任务取消和重试缺少所有权校验

- 位置：`server/api/tasks.py`
- 现象：接口只校验 `admin/operator` 角色，没有比较任务创建者。
- 影响：一个 operator 可以取消或重试另一个 operator 的任务。
- 修复要求：
  - 使用稳定用户 ID 保存任务所有者；
  - 执行 owner-or-admin 校验；
  - 对无权访问的对象统一返回 404 或产品约定的拒绝响应；
  - 被拒绝时不得改变任务状态或向 Agent 重新下发。

### SEC-005：Agent 文件传输没有聚合磁盘配额

- 位置：`agent/file_transfer.py`
- 现象：`max_bytes` 只限制单个文件，不统计已提交文件、临时分块、并发预留或磁盘剩余空间。
- 影响：被滥用的 operator 会话可以通过重复合法传输填满一个或多个 Agent 数据卷。
- 修复要求：
  - 对已提交、临时和预留字节进行持久化聚合统计；
  - 原子执行空间预留；
  - 设置最低剩余空间；
  - 取消、失败和过期会话必须释放预留并清理临时文件。

## P3：低危安全问题

### SEC-006：viewer 可以读取其他用户的文件传输历史

- 位置：`server/api/files.py`
- 现象：所有者过滤只应用于 operator，viewer 反而进入全局读取分支。
- 修复要求：除管理员外默认按所有者过滤；如确需全局只读视图，应使用单独的脱敏接口。

### SEC-007：登录耗时泄露用户名是否存在

- 位置：`server/api/auth.py`、`server/core/security.py`
- 现象：不存在的用户会短路跳过 bcrypt，存在用户的错误密码会执行完整校验。
- 修复要求：对不存在的用户使用同策略的固定虚拟哈希，保证每次登录尝试恰好执行一次密码校验。

### SEC-008：Docker 日志在截断前完整读入内存

- 位置：`agent/docker_runtime.py`
- 现象：`Process.communicate()` 完整缓存 stdout/stderr，之后才进行字符截断。
- 影响：被攻陷容器产生超长日志时，管理员读取日志可能造成 Agent 内存压力。
- 修复要求：并发分块读取双流，在解码前执行共享字节上限，超限或超时时终止并回收子进程。

### SEC-009：旧可选远程命令在截断前完整缓存输出

- 原位置：`agent/executor.py`（已删除）
- 现象：即使命令经过开关和精确白名单限制，输出仍由 `communicate()` 无界缓存。
- 修复结果：`agent/task_runner.py` 仅用 `create_subprocess_exec` 执行本地固定
  argv 配置档；Server 和 Agent 双开关默认关闭，输出有界，超时或取消会终止并
  排空子进程管道。

## 需要继续验证

### FOLLOW-001：登录限流状态可能无界增长

应用内失败登录字典缺少全局容量上限和统一过期清理。当前生产 Nginx 会覆盖转发 IP 并执行每 IP 限流，尚未证明单个攻击者可以产生足够多的源身份。应在真实部署中测量键数量，并优先改为有容量上限的 TTL 缓存。

### FOLLOW-002：Agent 请求结果没有绑定节点身份

状态：已修复。待处理 Future 同时保存预期 `node_id`，只有该节点返回相同
`request_id` 才能解析请求；跨节点伪造已加入回归测试。

## 已确认的功能缺陷

### DEFECT-001：更新包传输与 Agent WebSocket 上限冲突

服务端把更新包完整读入内存并 Base64 编码为单条 WebSocket 消息；约 1.5 MiB 的原始包就可能超过 Agent 的 2,000,000 字节接收上限。更新功能应迁移到分块、断点续传和逐块校验协议。

### DEFECT-002：浏览器终端未完成

`server/api/terminal.py` 的 Agent 输出监听未实现，`web-ui/src/views/Terminal.vue` 仍是模拟页面。

### DEFECT-003：部分前端页面仍是占位实现

状态：部分修复。`Tasks.vue` 已升级为正式任务中心；`Updates.vue` 和
`AuditLogs.vue` 仍未接入完整生产功能。

### DEFECT-004：独立启动脚本目录错误

`scripts/start-server.bat` 和 `scripts/start-webui.bat` 把目标解析到 `scripts` 子目录下，无法从项目根目录正确启动对应组件。

### DEFECT-005：更新回滚会残留新文件

`agent/updater.py` 在失败时只恢复被备份的旧文件，不会删除本次更新新创建的文件。

### DEFECT-006：文件分块并发写入缺少会话锁

状态：已修复。Agent 文件初始化、分块、提交、取消和配额预留统一使用会话锁，
相同 offset 不再并发通过长度检查。

### DEFECT-007：前端生产包过大

生产构建存在约 1.1 MiB 的大分块警告，需要按页面和大型依赖进行拆包。

### DEFECT-008：Python 依赖不可复现

`requirements.txt` 主要使用最低版本约束，没有锁定解析结果或依赖哈希。

## 已验证的安全控制

- REST 会话执行 `token_version` 撤销检查；
- Cookie、CSRF 和 Origin 控制已启用；
- Agent 注册令牌具备高熵、过期、撤销和一次性消费控制；
- Agent 凭据以哈希形式保存在服务端，轮换和撤销会断开在线连接；
- 文件传输包含相对路径、盘符、UNC、ADS、设备名、目录穿越和符号链接逃逸防护；
- 文件分块和完整文件执行 SHA-256 校验，并使用原子提交；
- Docker 操作使用参数数组和运行时 ID 白名单，没有 shell 字符串拼接；
- 批量命令默认关闭，要求 Server/Agent 双开关并只执行 Agent 本地固定 argv 别名；
- task v2 使用逐节点 execution_id、ACK、进度、结果和真实取消确认；
- 任务目标统一展开、去重、限制 32 个 Agent，并校验共同配置档和协议版本；
- 日志正式清理必须引用 30 分钟内参数与目标完全一致的成功预览；
- Agent 日志和备份路径拒绝绝对输入、`..`、符号链接与沙箱逃逸；
- WebSocket principal、撤销、过期和事件角色边界有自动化回归测试；
- Agent 文件接收器设置单文件上限、聚合上限和最低剩余空间；
- Docker 与可选命令输出在解码前执行共享字节预算；
- 生产部署中应用端口不直接发布，Nginx 负责 TLS，Server 容器以非 root 用户运行。

## 验证基线

- `pytest -q`：41 passed，1 skipped；
- `npm test`：2 个前端测试文件、5 个组件/store 测试通过；
- `npm run build`：通过，存在大分块警告；
- `npm audit`：0 个已知依赖漏洞；
- `python tests/e2e_task_runtime.py`：双真实 Agent 的配置档交集、健康检查、
  ZIP 哈希/解压、取消确认、部分暂停和指定节点重试通过；测试进程与临时文件已清理；
- `docker compose config -q`：通过（未设置本机 `WCM_PUBLIC_HOST` 时只有预期警告）；
- 静态危险模式复核：Agent 不再调用 `Process.communicate()`，服务端不再把 Agent payload 展开到可信事件字段。

## 敏感数据规则

以下内容不得进入 Git、Docker 构建上下文、发行包或问题报告：

- `.env` 和任何真实密码、令牌、API 密钥；
- `.claude/`、`.codex/` 等本地工具凭据配置；
- `secrets/`、私钥和本地证书；
- `server/data/` 中的 SQLite 数据库与迁移备份；
- `agent/agent_data/` 中的 Agent 注册凭据和运行日志；
- `server/logs/`、更新临时文件以及可能包含节点信息的诊断输出。

仓库只保留没有真实值的 `.env.example` 和其他明确标记的配置模板。发现凭据曾经进入 Git 历史时，删除当前文件并不足够，必须轮换凭据并对历史进行专门清理。
