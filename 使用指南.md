# Web Cluster Manager 前端使用指南

本文档对应当前 `3.1.0` 版本，说明哪些前端功能已经可用、如何使用，以及哪些菜单目前仍是占位页面。

## 1. 当前功能概览

| 页面 | 路由 | 当前状态 | 已实现能力 |
| --- | --- | --- | --- |
| 登录 | `/login` | 可用 | Cookie 会话登录、自动恢复会话、会话刷新、退出登录 |
| 仪表盘 | `/` | 部分可用 | 健康评分、在线/离线节点、活动告警数、CPU/内存/磁盘平均值 |
| 节点管理 | `/nodes` | 可用 | 宿主机与 Docker 容器树、搜索、状态筛选、容器指标、日志、启动/停止/重启 |
| 分组管理 | `/groups` | 基本可用 | 查看、创建、删除分组 |
| 任务中心 | `/tasks` | 占位 | 页面尚未接入已有任务 API |
| 文件传输 | `/files` | 可用 | 分块上传、SHA-256 校验、多 Agent 分发、逐节点进度与断点续传 |
| 远程终端 | `/terminal` | 演示/占位 | 只显示模拟输出，不会执行命令 |
| 监控大屏 | `/monitor` | 调试用途 | 查看前端 WebSocket 连接状态、最近消息、发送 Ping |
| 更新管理 | `/updates` | 占位 | 页面尚未接入已有更新包 API |
| 审计日志 | `/audit` | 占位 | 页面尚未接入已有审计查询和导出 API |
| 系统设置 | `/settings` | 可用 | 修改当前密码；管理员签发、查看和撤销 Agent 注册令牌 |

所有菜单对已登录用户可见，但敏感操作仍会由前端按钮和后端权限共同限制。

## 2. 登录与退出

### 当前本机地址

- 系统入口：`http://127.0.0.1:8000/login`
- 当前管理员用户名：`admin`
- 当前管理员密码文件：`secrets/local_admin_password.txt`

密码文件已被 `.gitignore` 忽略，不应提交到 Git、聊天记录或公开文档。

### 登录步骤

1. 打开 `http://127.0.0.1:8000/login`。
2. 输入管理员用户名和密码。
3. 登录成功后自动进入仪表盘。
4. 右上角用户菜单可退出登录。

系统使用 `HttpOnly Cookie` 保存会话，不再把访问令牌写入 `localStorage`。刷新页面后，前端会调用 `/api/v2/auth/me` 自动恢复登录状态。

如果代码刚更新但页面行为仍旧，先按 `Ctrl + F5` 强制刷新浏览器缓存。

## 3. 页面布局

登录后左侧显示功能菜单，顶部提供：

- 侧栏展开/折叠；
- 浅色/深色主题切换；
- 活动告警数量；
- 当前用户名和退出登录。

底部状态栏每 15 秒刷新一次集群健康信息，包括在线节点、离线节点、CPU、内存、磁盘和健康评分。

## 4. 仪表盘

仪表盘当前展示后端实时汇总数据：

- 集群健康评分；
- 在线、离线和维护中节点数量；
- 活动告警数量；
- 节点平均 CPU、内存和磁盘使用率。

“CPU 负载趋势”图表框架已经存在，但目前没有接入历史时间序列，因此可能显示为空图。这不代表 Agent 离线，可在“节点管理”中确认实时指标。

## 5. 节点与 Docker 容器

这是当前最完整的管理页面。

### 查看宿主机和容器

1. 进入“节点管理”。
2. 点击宿主机行左侧的展开箭头。
3. 子行会显示该宿主机 Agent 发现的 Docker 容器。

页面展示：

- 宿主机 IP、主机名、操作系统、Agent 版本和在线状态；
- 容器名称、镜像、运行状态、CPU 和内存指标；
- IP、主机名、容器名和镜像搜索；
- 宿主机在线、离线和维护状态筛选。

### 宿主机操作

- “维护”：把节点标记为维护状态；
- “移除”：仅管理员可用，会删除宿主机及其关联容器记录，不会删除真实设备。

### 容器操作

管理员可以：

- 查看最近 200 行日志；
- 启动容器；
- 停止容器；
- 重启容器。

操作由宿主机上的 Agent 调用本机 Docker CLI 执行。服务端不会挂载 Docker Socket，也不提供容器 `exec`。

## 6. 分组管理

当前支持：

- 查看分组列表和节点数量；
- 设置分组名称、描述和颜色；
- 创建分组；
- 删除分组。

后端已经提供“节点加入/移出分组”接口，但当前前端尚无对应操作控件。

## 7. 系统设置

### 修改密码

所有登录用户都可以修改自己的密码：

1. 输入当前密码；
2. 输入至少 12 位的新密码；
3. 点击“更新密码”。

密码修改后旧会话会失效，当前浏览器会获得新的有效会话。

### 注册新的 Agent

注册令牌功能仅管理员可用。

1. 填写设备备注，例如“机房 A Docker 宿主机”。
2. 设置有效期，范围为 1 到 1440 分钟。
3. 点击“生成令牌”。
4. 立即复制页面生成的命令。令牌原文只显示一次，且只能成功使用一次。
5. 在目标 Windows 或 Linux Docker 宿主机上执行命令。

Windows 开发环境示例：

```powershell
$env:WCM_ENROLLMENT_TOKEN = "一次性令牌"
python agent/main.py `
  --api "http://管理服务器:8000/api/v2" `
  --server "ws://管理服务器:8000/ws/agent"
```

Linux 开发环境示例：

```bash
export WCM_ENROLLMENT_TOKEN="一次性令牌"
python3 agent/main.py \
  --api "http://管理服务器:8000/api/v2" \
  --server "ws://管理服务器:8000/ws/agent"
```

生产环境必须使用内部 CA 签发的 HTTPS/WSS，并通过 `--ca-cert` 指定 CA 证书。运行 Agent 的系统账号必须有权执行 Docker CLI。

令牌列表会显示待使用、已使用、已撤销和过期状态；尚未使用的令牌可以手动撤销。

## 8. 监控大屏与 WebSocket

当前“监控大屏”是连接诊断页面，不是完整可视化大屏。它可以：

- 显示前端 WebSocket 是否连接；
- 显示最近收到的实时消息；
- 向服务端发送 Ping 测试。

节点指标和容器库存消息会通过 WebSocket 更新 Pinia 状态；容器库存变化会触发节点列表刷新。

## 9. 文件传输

“文件传输”用于把浏览器选择的文件安全分发到一个或多个在线 Agent：

1. 拖拽或选择一个不超过 100 MiB 的文件；
2. 选择一个或多个在线 Agent；
3. 输入 Agent 沙箱内的相对保存路径，例如 `packages/app.zip`；
4. 如确实需要替换已有文件，勾选“允许原子覆盖同名文件”；
5. 点击“上传并开始分发”。

浏览器会先计算完整 SHA-256，再以 512 KiB 分块上传到服务端。服务端校验
完整文件后才开始向 Agent 分发，Agent 也会逐块校验，并在完整哈希一致后把
临时文件原子移动到最终位置。

目标路径始终位于 Agent 的 `WCM_TRANSFER_ROOT`（默认
`agent_data/transfers`）下。系统拒绝绝对路径、Windows 盘符、UNC 路径、
`..`、NTFS 数据流和逃逸沙箱的符号链接。Docker 容器不是直接文件目标，
但运行在 Docker 宿主机上的 Agent 可以正常接收文件。

如果 Agent 断线、响应超时或服务重启，对应节点会显示为“已暂停”或“失败”。
恢复连接后点击记录右侧的“续传”，系统会从 Agent 已确认的字节偏移继续，
不会重新发送已经写入并校验的分块。服务端源文件默认保留 7 天，未完成上传
临时文件保留 24 小时。

## 10. 当前尚未完成的页面

以下菜单目前不要用于生产操作：

- 任务中心；
- 远程终端；
- 更新管理；
- 审计日志查看器。

这些模块的部分后端 API 和前端 API 封装已经存在，但页面仍是占位实现。特别是“远程终端”中的输入框只追加模拟文本，不会执行远程命令。远程命令能力默认关闭。

## 11. 权限说明

系统定义三种角色：

- `admin`：管理员，可管理 Agent 注册令牌、容器、节点、用户、告警规则、更新和审计日志；
- `operator`：操作员，可执行部分节点、分组、任务和文件操作；
- `viewer`：只读用户，主要用于查看节点、指标和状态。

即使手工调用接口，后端仍会验证角色，不依赖前端隐藏按钮来保证安全。

当前前端提供创建用户的 API 封装，但尚未实现用户管理页面。

## 12. 常见问题

### 登录成功后仍回到登录页

1. 按 `Ctrl + F5` 强制刷新；
2. 确认使用的是 `secrets/local_admin_password.txt` 中的当前密码；
3. 确认 `http://127.0.0.1:8000/api/v2/health` 返回 `status: ok`；
4. 清除该站点旧的浏览器缓存后重试。

### 看不到 Docker 容器

1. 确认 Docker Desktop 或 Docker Engine 正在运行；
2. 在 Agent 宿主机执行 `docker version` 和 `docker ps -a`；
3. 确认 Agent 在“节点管理”中为在线状态；
4. 检查 Agent 运行账号是否具有 Docker 权限；
5. 查看 `agent/agent_data/agent.err.log`。

### 容器日志或启动/停止失败

- 确认当前用户是管理员；
- 确认宿主机 Agent 在线；
- 确认容器仍存在于该宿主机；
- 检查 Docker Engine 状态和 Agent 错误日志。

### 仪表盘趋势图为空

当前版本尚未把历史指标写入趋势图数据源，这是已知的未完成功能。

## 13. 安全注意事项

- Pikachu、DVWA 等靶场包含故意保留的漏洞，只应绑定到本机或隔离内网；
- 不要把靶场端口暴露到公网；
- 不要提交 `secrets/`、Agent 凭据、SQLite 数据库或内部 CA 私钥；
- 注册令牌只应通过可信渠道传递；
- 生产环境必须使用 HTTPS/WSS；
- Docker 管理只开放日志和启动、停止、重启，不开放任意容器命令执行。
