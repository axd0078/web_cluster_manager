# Web Cluster Manager 4.0 使用指南

## 1. 身份与登录

系统只有两种身份：

- `user`：普通身份，面向日常监控和固定低风险操作。
- `admin`：管理员身份。普通登录只增加全局查看能力；危险写操作仍需要 5 分钟二次认证。

schema v7 首次迁移时，原 `admin` 保留，原 `operator`、`viewer` 和未知角色统一迁移为 `user`，同时所有账户的 `token_version` 增加 1，因此必须重新登录。

系统没有默认密码。首次启动使用部署者配置的管理员密码；不要把密码、Cookie、Agent/Broker 凭据或 SQLite 数据库提交到 Git。

登录后右上角会显示“普通用户”或“管理员”。管理员需要危险操作时：

1. 打开右上角用户菜单。
2. 点击“管理员授权（5 分钟）”。
3. 重新输入当前管理员密码。
4. 操作完成后可点击“撤销高权限授权”。

二次认证使用 HttpOnly Cookie，绑定当前用户、登录 `sid` 和 `token_version`，不能复制到另一次登录中使用。

## 2. 权限矩阵

| 能力 | user | admin 普通登录 | admin + 二次认证 |
| --- | --- | --- | --- |
| 节点、指标、容器清单、告警 | 可查看 | 可查看 | 可查看 |
| 确认告警 | 可 | 可 | 可 |
| 健康检查、日志预览、备份 | 本人任务 | 全部历史 | 全部历史 |
| 无覆盖文件传输 | 本人传输 | 全部历史 | 全部历史 |
| 固定低风险终端别名 | 可 | 可 | 可 |
| 审计、用户、设置、Broker 状态 | 不可 | 可查看 | 可查看 |
| 正式日志删除、服务重启、批量命令 | 不可 | 不可 | 可 |
| root/SYSTEM Shell | 不可 | 不可 | 可 |
| 容器日志和启停、文件覆盖 | 不可 | 不可 | 可 |
| 节点、分组、用户、凭据、规则、更新和设置写入 | 不可 | 不可 | 可 |
| 审计导出 | 不可 | 不可 | 可 |

危险任务的取消和重试沿用原任务风险级别。

## 3. 节点与 Docker 宿主机

“节点管理”显示已注册 Windows、Linux 和 Docker 宿主机 Agent，以及 Agent 发现的容器清单和资源指标。

Docker 容器不是独立 Agent，也不是文件、任务或终端目标。系统不提供容器内 `exec`。管理员二次认证后可以查看容器日志或执行启动、停止、重启；操作由宿主机 Agent 调用本地 Docker CLI。

## 4. Agent 注册

1. 管理员完成二次认证。
2. 进入“系统设置”，签发一次性 Agent 注册令牌。
3. 在目标设备设置 `WCM_ENROLLMENT_TOKEN`。
4. 使用 `agent/install.ps1`、`agent/install.sh` 或直接运行 `agent/main.py --enroll-only`。
5. Agent 保存独立凭据后，立即清除注册令牌。

生产环境 Agent 地址必须使用 `wss://.../ws/agent` 和 `https://.../api/v2`，并使用内部 CA 验证证书。

## 5. 任务中心

任务目标可以是节点、分组或全部在线 Agent，Server 会展开、去重并限制最多 32 个目标。五类任务为：

- 健康检查：低风险。
- 日志清理：预览为低风险；正式删除需要管理员二次认证，并引用 30 分钟内参数和目标完全一致的成功预览。
- 文件备份：低风险，ZIP 保存在 Agent 本地。
- 服务重启：高风险。
- 批量命令：高风险，只能执行 Agent 本地固定 `command_profiles`。

任务断线或确认超时时进入 `paused`，不会自动重发。恢复连接后由用户选择失败/暂停节点重试。普通用户只看本人任务；管理员可看全局任务。

## 6. 文件传输

1. 选择不超过 100 MiB 的文件。
2. 选择最多 32 个在线 Agent。
3. 输入 Agent `transfer_root` 内的相对路径。
4. 上传并创建分发任务。

浏览器先计算 SHA-256，再按 512 KiB 分块上传。Server 与 Agent 都校验分块和完整文件哈希，完成后原子提交。系统拒绝绝对路径、盘符、UNC、`..`、NTFS 数据流和符号链接逃逸。

普通用户与未二次认证的管理员不能覆盖已有文件。覆盖选项仅在 `files.overwrite` 权限生效时可用。

## 7. 普通安全终端

普通终端不是任意低权限 Shell，而是固定命令别名控制台。

### Agent 配置

复制平台示例到 `agent_data/task_profiles.json`，使用格式版本 2：

```json
{
  "version": 2,
  "log_profiles": {},
  "backup_profiles": {},
  "service_profiles": {},
  "command_profiles": {},
  "terminal_profiles": {
    "system-summary": {
      "argv": ["/usr/bin/uname", "-a"],
      "timeout": 15,
      "output_limit": 65536
    }
  }
}
```

然后同时启用 Server 与 Agent：

```text
WCM_ENABLE_LOW_TERMINAL=true
```

页面只允许选择并启动批准别名，Xterm.js 为只读状态。浏览器不能附加参数、工作目录、环境变量或自由输入。

## 8. 管理员 root/SYSTEM 终端

管理员终端只连接独立 Broker：

- Linux Broker 必须以 root 运行，固定启动 `/bin/bash --noprofile --norc`。
- Windows Broker 必须由 SYSTEM 任务启动，使用 ConPTY/pywinpty 固定启动无 Profile PowerShell。
- Broker 使用独立凭据连接 `/ws/broker`，与普通 Agent 凭据不通用。
- Broker 离线时绝不回退到 Agent。

使用步骤：

1. 管理员完成二次认证。
2. 确认目标节点的 Broker 在线。
3. 在“安全远程终端”选择“管理员 Shell”。
4. 阅读危险提示并确认。
5. 关闭后确认页面状态为 `closed`。

高权限会话每名管理员和每个节点最多一个，全局最多 16 个；空闲 10 分钟、最长 30 分钟。浏览器断线、认证撤销、Broker 断线、密码/角色变化或 Server 关闭都会终止目标进程。

## 9. 终端加密与审计

浏览器先通过 REST 获取 60 秒一次性票据，再连接 `/ws/terminal`，并在首条消息中提交票据。票据不进入 URL，消费后不能重放。

Server 与 Agent/Broker 每个会话生成临时 X25519 密钥，使用 HKDF-SHA256 派生上下行独立密钥，再用 ChaCha20-Poly1305 加密 `open/data/resize/close/exit/error` 帧。关联数据绑定协议版本、节点、会话、模式、方向和序号；重复、倒序、篡改或跨会话帧会被拒绝。

数据库只保存用户、节点、模式、时间、字节数、状态和结束原因，不保存命令、按键或输出正文。

## 10. 安装 Broker

管理员二次认证后，为节点轮换 Broker 凭据。原始凭据只显示一次。

Linux：

```bash
cd broker
python3 -m pip install -r requirements.txt
export WCM_BROKER_NODE_ID="节点 UUID"
export WCM_BROKER_TOKEN="一次性显示的 Broker 凭据"
export WCM_BROKER_SERVER_URL="wss://管理域名/ws/broker"
sudo -E ./install.sh
```

Windows（提升的 PowerShell）：

```powershell
cd broker
python -m pip install -r requirements.txt
.\install.ps1 `
  -NodeId "节点 UUID" `
  -BrokerToken "一次性显示的 Broker 凭据" `
  -ServerUrl "wss://管理域名/ws/broker"
```

安装完成后，在 Server 设置 `WCM_ENABLE_PRIVILEGED_TERMINAL=true` 并重启 Server。

## 11. 可信 Agent 更新中心

更新中心只更新注册的 Windows/Linux Agent。安装在 Docker 宿主机上的 Agent
按宿主机 OS 更新，但 Docker 容器不会成为更新目标；Server、前端和 Broker
也不通过该页面更新。

### 11.1 一次性 bootstrap

旧 Agent 不具备可信更新助手，必须在每台宿主机手工运行一次新版安装器，并
提供通过受控渠道取得的 `<key_id>.pub`：

Windows（提升的 PowerShell）：

```powershell
.\agent\install.ps1 `
  -ServerUrl "wss://管理域名/ws/agent" `
  -ApiUrl "https://管理域名/api/v2" `
  -CaCert "C:\secure\internal-ca.crt" `
  -UpdatePublicKey "C:\secure\<key_id>.pub"
```

Linux：

```bash
WCM_ENROLLMENT_TOKEN="一次性令牌" ./agent/install.sh \
  "wss://管理域名/ws/agent" \
  "https://管理域名/api/v2" \
  "/secure/internal-ca.crt" \
  "/secure/<key_id>.pub"
```

Windows 把程序、release 和可信公钥安装到
`%ProgramFiles%\WebClusterAgent`，把凭据、运行数据和 `update-spool` 放到
`%ProgramData%\WebClusterAgent`。Linux 分别使用
`/opt/web-cluster-agent` 和 `/var/lib/web-cluster-agent`。安装器会收紧
权限并创建独立的 root/SYSTEM 更新助手；Agent 账号不能修改 release、活动
版本指针或可信公钥。

确认新版 Agent 重新上线并上报 `update_protocol=2` 后，才设置
`WCM_ENABLE_AGENT_UPDATES=true`。可信公钥和特权更新助手本身不能通过 Agent
更新包升级，后续变更仍需手工 bootstrap。

### 11.2 构建与上传签名包

离线发布负责人按照 [tools/README.md](tools/README.md) 使用仓库外的
Ed25519 私钥生成 `.wcmupd`。每个包只对应一个 OS、规范化 CPU 架构和
Python ABI，并包含带哈希的 `requirements.lock` 与完整 wheelhouse。

操作员把 `<key_id>.pub` 同时部署到 Server 可信公钥目录和目标 Agent。签名
私钥绝不能上传到 Server、Agent、Git 或更新包中。

上传步骤：

1. 以管理员身份登录并完成 5 分钟二次认证。
2. 进入“更新管理”，上传不超过 100 MiB 的 `.wcmupd`。
3. 等待 Server 完成 Ed25519、清单、路径、大小和全部文件 SHA-256 校验。
4. 确认页面显示的版本、release ID、key ID、OS、架构和 Python ABI。

旧的无签名包显示为 `legacy_untrusted`，只能查看或删除，不能创建发布任务。

### 11.3 canary 发布

1. 选择更新包，再选择节点、分组或“全部在线节点”。
2. 点击“解析目标”，确认所有节点都在线、支持 update protocol 2，且环境与
   更新包精确匹配；最多 32 个目标。
3. 多节点任务至少选择一个 canary，并且必须保留至少一个非 canary 节点等待人工批准。单节点任务把该节点作为 canary，成功后
   直接完成。
4. 创建任务后查看逐节点传输、暂存、激活、健康检查和回滚状态。
5. 多节点 canary 全部健康后，任务进入 `awaiting_approval`；管理员重新确认
   二次认证并点击“批准剩余节点”，才会按最多四个节点一批继续。

Server 与 Agent 使用 512 KiB 分块、分块哈希和完整文件 SHA-256。断线或
Server 重启后从 Agent 已确认的偏移继续；单块最多自动重试三次，不把完整包
放进单条 WebSocket 消息。

### 11.4 取消、重试和回滚

- 任一 canary 或后续批次失败时，系统停止扩大发布范围。
- “取消”只阻止尚未激活的节点；正在原子切换的节点完成健康检查或回滚后才
  进入终态。
- 失败或暂停节点可以勾选后重试，每次尝试使用新的 execution ID。
- 新版本必须在 120 秒内以匹配的 release ID 重新连接并通过首轮健康检查；
  否则特权更新助手自动恢复上一活动 release。
- 对已成功节点可创建手动回滚任务；Agent 保留当前 release 和前两个已验证
  release。

更新历史只记录操作者、节点、版本、进度、错误和回滚结果，不保存私钥或包
内容。普通 `user` 不显示更新中心；未二次认证的 `admin` 只能查看包、
兼容性和历史。

## 12. 常见问题

### 终端显示“协议不兼容”

- 更新 Agent 到支持 terminal v3 的版本。
- 将 `task_profiles.json` 升级到 version 2。
- 检查 `terminal_profiles` 校验错误。
- 确认 Agent 启动环境包含 `WCM_ENABLE_LOW_TERMINAL=true`。

### 管理员终端不可用

- 确认使用 HTTPS/WSS。
- 确认 Server 的特权终端开关已开启。
- 确认目标 Broker 在线且凭据未撤销。
- 完成 5 分钟管理员二次认证。
- 检查该管理员或节点是否已有高权限会话。

### 普通用户看不到危险按钮

这是预期权限行为。普通身份没有危险操作权限；管理员也必须先完成二次认证，前端才显示相应操作。

### 登录后被要求重新登录

schema v7 迁移、密码修改、角色修改、账户禁用和退出都会使旧会话失效。

### 更新包显示“不受信任”

- 确认包由 `tools/update_package.py` 生成且离线 `verify` 成功。
- 确认 Server 的 `WCM_UPDATE_TRUSTED_KEYS_DIR` 中存在名称完全匹配的
  `<key_id>.pub`，容器用户对其有读取权限。
- 不要重签或手工修改 ZIP；任何清单或 payload 字节变化都会导致验签失败。

### 节点显示“更新协议不兼容”

- 旧 Agent 必须先完成一次手工 bootstrap，不能使用旧 Base64 更新接口升级。
- 确认 Agent 侧 `WCM_ENABLE_AGENT_UPDATES=true`，特权更新助手在线，并已
  上报 update protocol 2、updater 版本和活动 release ID。
- 确认包的 OS、架构和 Python ABI 与节点上报值精确一致。

## 13. 尚未完成的页面

审计日志页面仍是占位界面；对应后端接口保留权限保护，但不应把占位页面当作
完整生产工作流。更新管理和远程终端已不再是占位功能。
